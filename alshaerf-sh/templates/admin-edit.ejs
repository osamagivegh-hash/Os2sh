<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>تعديل الخبر</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
        }
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
        }
        .card-header {
            border-radius: 10px 10px 0 0 !important;
        }
        .btn-primary {
            background-color: #4e73df;
            border-color: #4e73df;
        }
        .btn-primary:hover {
            background-color: #2e59d9;
            border-color: #2e59d9;
        }
    </style>
</head>
<body>
    <nav class="navbar navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/admin">
                <i class="bi bi-arrow-right"></i> العودة للوحة التحكم
            </a>
            <span class="navbar-text">
                لوحة تحكم مدير الموقع
            </span>
        </div>
    </nav>

    <div class="container my-5">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card shadow">
                    <div class="card-header bg-primary text-white">
                        <h5 class="card-title mb-0">
                            <i class="bi bi-pencil-square"></i> تعديل الخبر
                        </h5>
                    </div>
                    <div class="card-body p-4">
                        <form action="/admin/edit-news/<%= newsItem._id %>" method="POST">
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">عنوان الخبر</label>
                                    <input type="text" name="title" class="form-control" value="<%= newsItem.title %>" required>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">الفئة</label>
                                    <input type="text" name="category" class="form-control" value="<%= newsItem.category || 'عائلة' %>">
                                </div>
                            </div>
                            
                            <div class="mb-3">
                                <label class="form-label">محتوى الخبر</label>
                                <textarea name="content" class="form-control" rows="8" required><%= newsItem.content %></textarea>
                            </div>
                            
                            <div class="row">
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">رابط الصورة</label>
                                    <input type="url" name="imageUrl" class="form-control" value="<%= newsItem.imageUrl || '' %>" placeholder="https://example.com/image.jpg">
                                    <% if (newsItem.imageUrl) { %>
                                        <div class="mt-2">
                                            <img src="<%= newsItem.imageUrl %>" alt="صورة الخبر" class="img-thumbnail" style="max-height: 150px;">
                                        </div>
                                    <% } %>
                                </div>
                                <div class="col-md-6 mb-3">
                                    <label class="form-label">اسم الكاتب</label>
                                    <input type="text" name="author" class="form-control" value="<%= newsItem.author || 'مدير الموقع' %>">
                                    
                                    <div class="mt-3">
                                        <div class="form-check form-switch">
                                            <input class="form-check-input" type="checkbox" name="isPublished" id="isPublished" 
                                                <%= newsItem.isPublished ? 'checked' : '' %> value="on">
                                            <label class="form-check-label" for="isPublished">
                                                نشر الخبر
                                            </label>
                                        </div>
                                        <small class="form-text text-muted">
                                            إذا تم تفعيل هذا الخيار، سيظهر الخبر للزوار. إذا لم يتم تفعيله، سيبقى كمسودة.
                                        </small>
                                    </div>
                                </div>
                            </div>
                            
                            <div class="d-flex justify-content-between align-items-center mt-4">
                                <div>
                                    <button type="submit" class="btn btn-success px-4">
                                        <i class="bi bi-check-circle"></i> حفظ التعديلات
                                    </button>
                                    <a href="/admin" class="btn btn-secondary ms-2">
                                        <i class="bi bi-x-circle"></i> إلغاء
                                    </a>
                                </div>
                                
                                <div class="text-muted">
                                    <small>
                                        تم الإنشاء: <%= newsItem.createdAt ? new Date(newsItem.createdAt).toLocaleString('ar-SA') : 'غير معروف' %>
                                    </small>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <footer class="bg-dark text-white text-center py-3 mt-5">
        <div class="container">
            <p class="mb-0">© 2024 موقع العائلة - جميع الحقوق محفوظة</p>
        </div>
    </footer>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>